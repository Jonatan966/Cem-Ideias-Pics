name: Deploy via FTP
on: [push]
jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Upload ftp
      uses: sebastianpopp/ftp-action@releases/v2
      with:
        host: ${{ secrets.HOSPEDAGEM }}
        user: ${{ secrets.FTP_USER }}
        password: ${{ secrets.FTP_PWD }}
        localDir: "site"
        remoteDir: "."
